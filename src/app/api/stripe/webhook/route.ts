import { NextResponse } from "next/server";
import type Stripe from "stripe";
import { headers } from "next/headers";
import { getStripeClient } from "@/lib/stripe";
import { createAdminClient } from "@/lib/supabase/admin";

export async function POST(request:Request){const signature=(await headers()).get("stripe-signature");const secret=process.env.STRIPE_WEBHOOK_SECRET;if(!signature||!secret)return NextResponse.json({error:"Webhook not configured."},{status:503});const body=await request.text();let event:Stripe.Event;try{event=getStripeClient().webhooks.constructEvent(body,signature,secret)}catch{return NextResponse.json({error:"Invalid Stripe signature."},{status:400})}try{if(event.type==="checkout.session.completed"){const session=event.data.object as Stripe.Checkout.Session;const orderId=session.metadata?.order_id;if(orderId){const admin=createAdminClient();const {data:order}=await admin.from("orders").select("id,total,amount_paid,currency,customer_id,provider_id").eq("id",orderId).single();const paid=Number(session.amount_total||0)/100;const newAmount=Math.min(Number(order.total),Number(order.amount_paid||0)+paid);const paymentStatus=newAmount>=Number(order.total)?"paid":"partially_paid";const intent=typeof session.payment_intent==="string"?session.payment_intent:session.payment_intent?.id||null;await admin.from("payments").upsert({order_id:order.id,customer_id:order.customer_id,provider_id:order.provider_id,stripe_checkout_session_id:session.id,stripe_payment_intent_id:intent,payment_kind:session.metadata?.payment_kind||"full",amount:paid,currency:String(order.currency||"EUR").toUpperCase(),status:"paid",paid_at:new Date().toISOString()},{onConflict:"stripe_checkout_session_id"});await admin.from("orders").update({amount_paid:newAmount,payment_status:paymentStatus,stripe_checkout_session_id:session.id,stripe_payment_intent_id:intent,status:paymentStatus==="paid"?"confirmed":"deposit_paid"}).eq("id",order.id);await admin.from("notifications").insert({user_id:order.provider_id,kind:"payment_received",title:"Payment received",body:`A customer paid ${paid.toFixed(2)} ${String(order.currency||"EUR").toUpperCase()} for an order.`,link:"/account?section=orders"});}}return NextResponse.json({received:true})}catch(error){return NextResponse.json({error:error instanceof Error?error.message:"Webhook processing failed."},{status:500})}}
